import React from 'react'
import styles from '@/styles/user-comment.module.css'

export default function UserComment() {
  return (
    <>
      {/* test
      <div>
        <div>
          {data.length &&
            data.map((v, i) => {
              return (
                <div key={v.course_id}>
                  <div className={styles.h2 + ' ' + styles.courseName}>
                    id:動態路由,{v.teacher_name}
                    id:動態路由,{v.teacher_name}
                  </div>
                  <div className={styles.h2 + ' ' + styles.courseName}>
                    name:動態路由,name:{v.course_name}
                  </div>
                </div>
              )
            })}
        </div>
      </div>
      */}
    </>
  )
}
